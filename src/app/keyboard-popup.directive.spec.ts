import { KeyboardPopupDirective } from './keyboard-popup.directive';

describe('KeyboardPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardPopupDirective();
    expect(directive).toBeTruthy();
  });
});
